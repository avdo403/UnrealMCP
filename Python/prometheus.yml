global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: 'unreal_mcp'
    static_configs:
      - targets: ['host.docker.internal:8000']
        labels:
          service: 'unreal_mcp_server'
